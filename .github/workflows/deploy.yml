name: Deploy to GitHub Page

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Install Dependencies
      run: npm ci

    - name: Deploy
      run: npm run deploy

permissions:
  contents: write
